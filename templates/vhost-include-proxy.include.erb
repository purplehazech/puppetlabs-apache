ProxyPass <%= location %> <%= dest %>
<Location <%= location %>>
        ProxyPassReverse /
        <% if url_map != '' %>
        SetOutputFilter proxy-html
        ProxyHTMLURLMap <%= url_map %>
        <% end %>
        RequestHeader set X_FORWARDED_PROTO "https"
        RequestHeader set Accept-Encoding ""
</Location>

